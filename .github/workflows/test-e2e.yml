name: Test e2e
on: 
  push:
  workflow_dispatch: {}
  pull_request:
    types: [opened, synchronize, reopened]
 
jobs:
  test-e2e:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
